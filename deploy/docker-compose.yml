# Reference:
# - https://docs.docker.com/compose/compose-file
# - https://eclipsesource.com/blogs/2018/01/11/authenticating-reverse-proxy-with-keycloak/

version: '3.5'

services:
  # Exposed server that's handling incoming web requests
  # Underlying image: openresty/openresty:alpine-fat
  atlas_zmed_viewer:
    build:
      # Project root
      context: ./../
      # Relative to context
      dockerfile: ./deploy/dockerfile
      args:
        - ZVIEWER_HOST=${ZVIEWER_HOST}
        - ZVIEWER_APP_CONFIG=${ZVIEWER_APP_CONFIG}
    image: atlas_zmed_webapp:latest
    container_name: atlas_zmed_webapp
    volumes:
      # Nginx config
      - ./config/${ZVIEWER_HOST}.nginx.conf:/usr/local/openresty/nginx/conf/nginx.conf:ro
      # Logs
      - ./logs/nginx:/var/logs/nginx
      # Ssl Encrypt
      - ./certs:/etc/letsencrypt
      - ./certs:/var/www/letsencrypt
    ports:
      - '6443:443' # SSL
      - '9080:80' # Web
    depends_on:
      - atlas_zmed_keycloak
      # - orthanc
    restart: on-failure

  # LINK: https://hub.docker.com/r/jodogne/orthanc-plugins/
  # TODO: Update to use Postgres
  # https://github.com/mrts/docker-postgresql-multiple-databases
  # orthanc:
  #   image: jodogne/orthanc-plugins:1.5.6
  #   hostname: orthanc
  #   container_name: orthanc
  #   volumes:
  #     # Config
  #     - ./config/orthanc.json:/etc/orthanc/orthanc.json:ro
  #     # Persist data
  #     - ./volumes/orthanc-db/:/var/lib/orthanc/db/
  #   restart: unless-stopped

  # LINK: https://hub.docker.com/r/jboss/keycloak
  atlas_zmed_keycloak:
    image: adorsys/keycloak:6.0.1
    hostname: atlas_zmed_keycloak
    container_name: atlas_zmed_keycloak
    volumes:
      # Theme: https://www.keycloak.org/docs/latest/server_development/index.html#_themes
      - ./volumes/keycloak-themes/ohif:/opt/jboss/keycloak/themes/ohif
      # Previous Realm Config
      - ./config/${ZVIEWER_KEYCLOAK_CONFIG}:/tmp/ohif-keycloak-realm.json
      # ssl
      - ./certs/${ZVIEWER_HOST}/${ZVIEWER_HOST}.crt:/etc/x509/https/tls.crt
      - ./certs/${ZVIEWER_HOST}/${ZVIEWER_HOST}.key:/etc/x509/https/tls.key
    environment:
      # Database
      DB_VENDOR: postgres
      DB_ADDR: atlas_zmed_postgres
      DB_PORT: 5432
      DB_DATABASE: keycloak
      DB_SCHEMA: public
      DB_USER: keycloak
      DB_PASSWORD: password
      # Keycloak
      KEYCLOAK_USER: admin
      KEYCLOAK_PASSWORD: secret-password
      KEYCLOAK_IMPORT: /tmp/ohif-keycloak-realm.json -Dkeycloak.profile.feature.upload_scripts=enabled
      # KEYCLOAK_WELCOME_THEME: <theme-name>
      # KEYCLOAK_DEFAULT_THEME: <theme-name>
      # KEYCLOAK_HOSTNAME: https://app.zmed.z-union.ru/
      # KEYCLOAK_LOGLEVEL: DEBUG
      KC_HOSTNAME_URL: https://${ZVIEWER_HOST}/auth
      PROXY_ADDRESS_FORWARDING: 'true'
    ports:
      - "18444:8443"
      - "18081:8080"
      - "19991:9990"
    depends_on:
      - atlas_zmed_postgres
    restart: unless-stopped

  # LINK: https://hub.docker.com/_/postgres/
  atlas_zmed_postgres:
    image: postgres:11.2
    hostname: atlas_zmed_postgres
    container_name: atlas_zmed_postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    # expose:
    #   - "5433" # Publishes 5433 to other containers but NOT to host machine
    # ports:
    #   - "5433:5433"
    environment:
      POSTGRES_DB: keycloak
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: password
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local
